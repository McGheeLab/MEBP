# Core application dependencies
PySide6==6.11.1
pyserial==3.5
pygame==2.6.1
numpy==2.4.6   # capped <2.5 by numba (pulled via pylablib for the Andor Zyla backend)
scipy==1.18.0
opencv-python==5.0.0.93
Pillow==12.3.0
pandas==3.0.3
scikit-image==0.26.0

# Windows camera identity (DirectShow enumeration) - Windows only
comtypes==1.4.16; sys_platform == "win32"
pygrabber==0.2; sys_platform == "win32"

# ANDOR Zyla (SDK3) microscope camera backend — Windows rigs with an Andor sCMOS.
# pylablib drives the camera; it pulls numba + llvmlite (JIT acceleration), and
# numba caps numpy to <2.5 (hence the numpy==2.4.6 pin above). Requires the Andor
# SDK3 runtime DLLs (atcore.dll + companions) in DLLs/zyla dlls/ (vendored) or an
# Andor SDK3 / Solis install. The backend (gui/widgets/andor_backend.py) is
# lazily guarded, so the app still runs if pylablib/DLLs are absent.
pylablib>=1.4.3; sys_platform == "win32"
numba>=0.60; sys_platform == "win32"
llvmlite>=0.43; sys_platform == "win32"

# Motorized microscope body (Nikon Ti Eclipse) — OPTIONAL.
# Only the "micromanager" backend of SupportClasses/MicroscopeControl.py needs
# this; the import is guarded, so the app runs fine without it (the Nikon SDK
# and simulated backends are unaffected). Deps are just numpy, which is already
# pinned above. NOTE: pymmcore is the MMCore engine only — driving a real Ti
# ALSO needs Micro-Manager's device adapters AND Nikon's own driver/SDK, which
# Micro-Manager's adapter wraps rather than replaces (see micro-manager.org/NikonTI).
pymmcore==12.5.0.75.0

# .nd3 container format (SupportClasses/ND3.py) — HDF5-based image container
# shared with Blender / LabLink readers. Wheels only, no compiler needed; does
# NOT move the numpy pin (h5py needs numpy>=1.21.2). PyInstaller detects h5py
# automatically via its bundled hook.
h5py>=3.12

# Packaging (build_exe.py)
pyinstaller==6.21.0
