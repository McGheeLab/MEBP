"""gui.pages.workflows — v7.4.3 Workflows mode sub-pages.

The Workflows mode lands users on a picker (`workflow_picker.py`) that
chooses between named workflows. Each workflow page composes a curated
set of tools (`gui/widgets/live_target_picker.py`, ...) to complete a
specific task. Workflows are modal — commit to one, then Back to the
picker — not parallel tabs.
"""
