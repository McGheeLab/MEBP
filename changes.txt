The calibration left box does not auto update the plate configuration: format 
the well setup does not recognize the list of current inks
the well setup does not recognize the list of current rosettes
the X and Y movements on the jog page do not show the correct motion amount for the step size requested on the jog settings
on the hardware setup page I want the left box to have a list of the available harware setup files we currently have 
in well setup I dont need to see the projections XZ and ZY
the well view in the print object pannel needs work, the projections should match the XY view. axis should alllign. hard to see since its so small. objects in print pannel should be to the right of the well preview. we should be able to expand the well preivew window and zoom in and everything. 
If a print is outside the print area of a well preview, it should be flashing red in the objects in this print section
All over the titles of each section are not in the correct allignment 
Inks should be assigned to pumps
If the needle is multichannel then each channel must be assigned to a pump a single needle bore must also be assigned to a pump therefor the needle selection should go after the pump assignments. 
validate the well setup before sending the print to the monitor. To be valid it must have correct ink assignments (if pump is incremental then the ink assigned to the pump should be in a well).
